# utils

Runtime logging helper. `runtime_logger.hpp` (C++) and `runtime_logger.py`
(Python) are a matching pair, included and imported directly with no build step.

Set `NLCELL_RUNTIME_LOG=/path/to/logs` to collect each tool's
`<tool>.runtime.log` file in that directory.
