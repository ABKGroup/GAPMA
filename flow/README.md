# flow

Entry scripts for the GAPMA pipeline.

```bash
cd flow
./datagen.sh   --config config/datagen.conf
./train_gat.sh --config config/train_gat.conf
./autorun.sh   --config config/autorun.conf
```
