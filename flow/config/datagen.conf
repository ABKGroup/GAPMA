
REPO="${REPO:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)}"
OUT_ROOT="$REPO/data/training"
PYTHON_BIN="python3.9"
STAGE="route_opt"

DESIGNS=(
  "gcd|local|200|1"
  "picorv32|local|400|1"
  "ibex_core|local|400|1"
  "blake2s_core|local|300|1"
  "poly1305_core|local|300|1"
)

MINE_MIN_CELLS=2
MINE_MAX_CELLS=8
MINE_MIN_INPUTS=2
MINE_MAX_INPUTS=4
MINE_MAX_DEPTH=3
MINE_MAX_OUTPUTS=1
MINE_NUM_CORES=4
